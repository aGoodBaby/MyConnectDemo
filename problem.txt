1.onpostexcute()里代码太多，任何需要对titlelist相关的操作都要在这里处理。
    这样是否合适？不合适的话如何解决？

    答:onpostexcute()本来就是在主线程执行的,所以不用担心耗时操作多(嗨,谁说子线程中代码不能多了,你shader写多了吧)
    (doinbackground()才是子线程操作啊,你记错了)现在你的问题就是onpostexcute()如何更新呢?正常监听器可以直接等它
    结束,如果是其他子线程呢?所以得给线程排个序,不过这样又跟一个线程没区别,但也只能这样了。



    随想:如果需要子线程回调一个值来更新ui(正常来说可以在handler处提交),但是回调接口的函数因为是放在子线程中,所以回调函
    数内并不能更新ui,如果通过设置一个标记来判断的话,activity又没有一个函数是时刻调用的,可能可以学习一下那些监听器
    是怎么做到的,但我估计他同样是靠回调函数解决的,只不过他的回调函数是在主线程可以直接改变ui(哈哈,一不小心把监听器
    的原理猜出来了),所以目前看,只能通过post来解决了,代码多点就多点吧,只要减少代码耦合(android也并不需要很多数据相
    关,一个界面游戏而已)就够了。
